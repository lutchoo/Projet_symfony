{% extends 'base.html.twig' %}

{% block title %}Edit Boardgame{% endblock %}

{% block body %}
<section class = "container">
    <h1>Edit Boardgame</h1>

    {{ include('game/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="m-5 btn btn-warning" href="{{ path('app_game_index') }}">Retour à la liste</a>

    <a class="btn btn-danger">{{ include('game/_delete_form.html.twig') }}</a>
</section>
{% endblock %}
